# Scenario: error — immediate HTTP error, no stream at all.
# 400 is deliberate: tina retries 5xx/429 with jittered backoff (random
# timing), but a 400 fails the turn at once and deterministically. The body
# is the OpenAI error shape. The step has no SSE framing — every non-#
# line is sent verbatim as the body.
!status 400
{"error":{"message":"stub: scenario error — provider rejected the request","type":"invalid_request_error","code":"stub_error"}}
