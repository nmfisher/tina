name: attractor
description: A DOT-based pipeline runner — directed graphs (Graphviz DOT) that orchestrate multi-stage agent workflows. UI- and LLM-agnostic; consumed by tina.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.12.0

dependencies:
  logging: ^1.3.0
  path: ^1.9.0

dev_dependencies:
  test: ^1.25.0
