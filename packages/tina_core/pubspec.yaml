name: tina_core
description: Shared value types — messages, tool schemas, stream events — and the LlmProvider interface. The dependency-free seam a provider package and a loop package can both depend on without depending on each other.
publish_to: none
version: 0.1.0
environment:
  sdk: ^3.3.0
