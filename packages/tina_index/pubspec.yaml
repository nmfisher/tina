name: tina_index
description: AST-derived dependency graph for Dart codebases — symbol extraction, typed edges, graph traversal, and keyword seeding.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.0.0

dependencies:
  analyzer: ^13.0.0
  path: ^1.9.0

dev_dependencies:
  test: ^1.25.0
