name: dart_wasmtime
description: Vendored Wasmtime C API for tina's WASM plugin runtime.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  code_assets: ^1.0.0
  ffi: ^2.1.0
  hooks: ^1.0.0
  native_toolchain_c: ^0.17.1
  path: ^1.9.0

dev_dependencies:
  lints: ^1.0.0
  test: ^1.31.0
