name: tina_engine_2
description: >-
  A standalone review artifact: one agent loop, one plugin interface, one
  scripted provider. Not wired into tina. Dart standard library only.
publish_to: none
version: 0.1.0
environment:
  sdk: ^3.6.0
dev_dependencies:
  test: ^1.25.0
