name: file_tree
description: Bounded file inventories, tree hashes and change detection with caller-owned collection policy.
publish_to: none
version: 0.1.0
environment:
  sdk: ^3.12.0
dependencies:
  crypto: ^3.0.7
  path: ^1.9.0
dev_dependencies:
  test: ^1.25.0
