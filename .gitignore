# IDE / editor settings
.claude/
.poolside/

# Release artifacts
releases/bundle/
releases/*.tar.gz
releases/*.sha256
.tina/

# Dart build output. These are per-checkout and machine-specific — large
# (a test snapshot is ~26 MB) and regenerated by `dart pub get` / `dart test`.
# Never commit them: `git add -A <package>` will happily stage them otherwise.
.dart_tool/
build/
