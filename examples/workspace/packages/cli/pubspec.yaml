name: cli
description: The `track` command-line app wiring core + store + reports.
publish_to: none
version: 0.3.0

environment:
  sdk: ^3.12.0

dependencies:
  core:
    path: ../core
  reports:
    path: ../reports
  store:
    path: ../store
